# Seed Navigator Prototype

A lightweight static prototype for helping residents discover what they need to grow seeds from a local library collection.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
